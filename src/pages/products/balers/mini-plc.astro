---
import Layout from "../../../layouts/Layout.astro";
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
import { Image } from "astro:assets";
import SecondaryNav from "../../../components/SecondaryNav.astro";
import Product from "../../../components/Product.astro";

import productData from "../../../products.json";

const balerNames = productData.balers.types;

const balers = await Astro.glob("../../../images/Products/Balers/*");

const realPhotos = await Astro.glob("../../../images/Real Photos/Mini PLC/*");
---

<Layout canonical={canonicalURL} title="Mini PLC Hydraulic Baler Machine: Scrap Metal Press Baling Machine" description={productData.balers["mini_plc"].description}>
	
	<Product faq={productData.balers.faq} type="Baler" productData={productData.balers["mini_plc"]} image={[balers[6], balerNames[6]]}
	realPhotos={realPhotos} />
</Layout>
